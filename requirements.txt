# ============================================================
#  모아버거 AI 음성주문 키오스크 - Python 의존성
#  설치:  pip install -r requirements.txt
# ============================================================

# --- 웹 서버 (백엔드 프록시 + 정적 파일 서빙) ---
Flask==3.0.3
flask-cors==4.0.1
python-dotenv==1.0.1

# --- OpenAI (Whisper STT + GPT 메뉴 매칭) ---
openai==1.51.2

# --- Supabase (메뉴/주문 저장) ---
supabase==2.7.4

# --- 날씨 API 호출 및 HTTP 통신 ---
requests==2.32.3

# --- 프로덕션 배포용 WSGI 서버 (선택) ---
waitress==3.0.0
