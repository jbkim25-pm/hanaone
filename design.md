# 디자인 · UX 문서 (design.md)

모아푸드코트 키오스크의 **디자인/UX** 문서입니다. 목표는 **시니어와 20대를 동시에 만족**시키는 것 —
트렌디한 톤은 20대 기준, 글씨·대비·버튼 크기는 시니어 기준으로 **상향** 적용합니다.
(트렌디함과 접근성은 상충하지 않는다 — 지침 4장)

---

## 1. 디자인 원칙 (지침 3·7장)

1. **선명함 우선** — 높은 대비, 큰 글씨, 큰 버튼, 선명한 메뉴 이미지.
2. **음성 가능성의 명시적 노출** — 첫 화면에서 "말로 주문할 수 있어요"를 크게.
3. **확인 중심 흐름** — 각 단계에서 "무엇이 담겼는지"를 항상 크게 보여줌.
4. **실패해도 막히지 않음** — 음성 실패 시 유사추천/터치로 부드럽게 유도.
5. **이중 입력 동등성** — 음성으로 되는 건 터치로도 된다.

---

## 2. 컬러 토큰 (`static/css/style.css` `:root`)

| 토큰 | 값 | 용도 |
|------|-----|------|
| `--orange` / `--orange-dark` | `#FF5A1F` / `#E8410A` | 브랜드 포인트, 주요 버튼 |
| `--peach-bg` / `--peach-card` | `#FDEAE0` / `#FFF3EC` | 배경, 메뉴 썸네일 |
| `--ink` / `--ink-soft` | `#1E1B29` / `#6B6577` | 본문/보조 텍스트 |
| `--blue` | `#2E7DF6` | ICE(시원) 계열, 정보 |
| `--green` | `#1FA463` | 매칭 성공, 추천 뱃지 |
| `--purple` | `#7C6BF0` | 음성 도우미(AI) 아이덴티티 |
| `--dark` / `--dark-2` | `#241E33` / `#332A47` | 음성화면·하단바 |

- **대비**: 본문 텍스트/배경 명도 대비 4.5:1 이상(WCAG AA), 주요 버튼은 흰 글씨/진한 오렌지로 7:1 지향.
- **색상 의존 금지**: HOT/ICE는 색 + 아이콘(🔥/🧊) + 텍스트를 함께 사용.

---

## 3. 타이포그래피 (지침 7.1)

- 서체: **Noto Sans KR** (가독성 높은 산세리프, 얇은 굵기 지양).
- 최소 크기 규칙:

| 요소 | 크기 | 굵기 |
|------|------|------|
| 히어로 타이틀 | 72px | 900 |
| 메뉴 이름 | 26px | 900 |
| 가격 | 26px | 900 |
| 버튼 라벨 | 26~28px | 800 |
| 안내/자막 | 22~26px | 700~800 |
| 본문 최소 | 20px | — (이보다 작게 금지) |

---

## 4. 버튼 · 터치 타겟 (지침 7.3)

- 주요 버튼(`.btn-jumbo`) **최소 높이 80px**, 일반 버튼 56~64px 이상.
- `＋`(담기) 버튼 60×60px, 수량 스테퍼 버튼 52px.
- 버튼 간 간격 ≥ 12px(오터치 방지). 누를 때 `scale(.97)` 피드백.
- 핵심 조작부(마이크·결제)는 화면 **하단~중앙**에 배치(휠체어·저신장 배려, 지침 8장).

---

## 5. 화면별 디자인

### 5-1. 시작 화면 (`#screen-start`)
- 펄스 애니메이션 마이크 히어로 + "AI 주문 도우미" 태그로 **음성 가능성**을 즉시 인지.
- "말로 주문할 수 **있어요**"(오렌지 강조) 대형 타이틀.
- 두 개의 점보 버튼: 🎤 말로 주문 / 👆 화면 눌러서 주문(동등한 시각 위계).
- 안심 배지: "사투리도 괜찮아요 · English OK · 천천히 하셔도 돼요".
- 하단 고정바: "화면을 터치하거나 말씀해 주세요"(녹색 점 깜빡임).
- 음성 미지원 시 🎤 버튼은 자동으로 숨겨져 혼란 방지.

### 5-2. 메인 주문 화면 (`#screen-order`)
- 상단: 로고 + "처음으로".
- **날씨 배너**(보라 톤): "오늘은 28℃, 시원한 메뉴 어떠세요?" + 추천 칩(아이스 메뉴).
- **카테고리 탭**: 버거/사이드·디저트/음료/커피/스무디·티 — 아이콘+한글, 현재 탭 오렌지 강조.
- **메뉴 그리드**: 2열 카드, 큰 썸네일 + 이름(한/영) + 가격 + HOT/ICE 뱃지, 카드 전체가 터치 타겟.
  - 인기(오렌지)·추천(초록) 뱃지.
- **떠 있는 마이크 버튼**(우하단, 보라, 펄스) + "말로 주문" 라벨 상시 노출.
- **하단 장바구니 바**(다크): 담긴 개수·총액 크게 + "주문하기".

### 5-3. 음성 주문 화면 (`#screen-voice`)
- 다크 배경으로 몰입, 상단 상태 필: "듣고 있어요"(빨간 점).
- 파형 애니메이션 + 보라 마이크로 "듣는 중"을 시각화.
- "이렇게 들었어요" → 인식 문장 카드(손님 말씀)로 **확인 중심**.
- 하단 시트: 매칭 카드(초록 "✓ 메뉴를 찾았어요") + 온도 토글 + 수량 스테퍼 +
  "🎤 다시 말할게요" / "🛒 네, 담아주세요".
- 항상 "👆 화면을 눌러서 직접 고르셔도 돼요" 폴백 노출.

### 5-4. 장바구니 / 확인 / 완료
- 장바구니: 항목별 큰 썸네일·이름·온도·금액, 수량 스테퍼·삭제, 총합 강조.
- 확인: 큰 글씨로 항목·금액 나열, "결제하기"가 가장 강조.
- 완료: ✅ + **대형 주문번호**(예: `A-042`) + 10초 카운트다운 후 자동 초기화.

---

## 6. 음성 상태 표현 (지침 6.3)

| 상태 | 화면 | 문구(자막 동시) |
|------|------|-----------------|
| 대기 | 마이크 펄스 | "말로 주문할 수 있어요" |
| 듣는 중 | 파형+마이크 확대 | "듣고 있어요" |
| 처리 중 | 파형 정지 | "주문을 확인하고 있어요…" |
| 성공 | 매칭 카드 하이라이트 | "'○○ 한 잔' 맞으실까요?" |
| 실패 | 부드러운 안내 | "다시 말씀해 주시거나 화면을 눌러 주세요" |

모든 음성 안내는 **자막으로 동시 표시**(청각 약자·소음 환경 대응, 지침 8장).

---

## 7. 접근성 체크 (지침 8장)

- 음성 안내 + 자막 병행 · 명확한 초점 이동 · 크고 명확한 "이전/처음으로".
- 오류 문구는 비난하지 않게: "잘못 입력" ✕ → "다시 도와드릴게요" ○.
- 타임아웃 관대: 완료 화면 10초 카운트다운(운영 시 무입력 자동초기화는 90초+ 권장).
- 색상에만 의존하지 않는 정보 전달(아이콘·텍스트 병행).

---

## 8. 이미지 가이드

- 현재 데모는 **이모지 썸네일**로 구성(자산 없이 즉시 실행 가능).
- 운영 전환 시: 각 메뉴 `image_url`(또는 `menu.json > imageUrl`)에 800×800px 이상,
  배경 정돈된 고해상도 실사/렌더(WebP 우선, PNG 폴백) 지정 → 코드가 자동으로 이미지 우선 렌더.
- 로딩 실패 시 이모지/메뉴명으로 공백 방지.

---

## 9. 반응형

- 기준 캔버스 max-width **1080px**(세로형 키오스크). `#app` 중앙 정렬.
- 좁은 화면(≤520px)에서 히어로/그리드 크기 자동 축소(`@media`).
- 키오스크는 브라우저 전체화면(F11) 권장.
